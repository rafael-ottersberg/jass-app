import { Typography } from '@mui/material';
import React from 'react';

const HomeCard = () => {
  return (
    <div>
      <Typography>Hello from here</Typography>
    </div>
  );
};

export default HomeCard;
